<%= form_with(model: room_user) do |form| %>
  <% if room_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room_user.errors.count, "error") %> prohibited this room_user from being saved:</h2>

      <ul>
        <% room_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
